{
  "name": "cortex",
  "version": "1.0.0",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "start": "ts-node src/index.ts",
    "build": "tsc",
    "test:compile": "tsc --project tsconfig.test.json",
    "test:run": "node --test dist-tests/tests/**/*.test.js",
    "test": "npm run test:compile && npm run test:run",
    "test:watch": "tsc --project tsconfig.test.json --watch & node --test --watch dist-tests/tests/**/*.test.js",
    "test:debug": "node --inspect-brk --test dist-tests/tests/**/*.test.js",
    "test:coverage": "node --test --experimental-test-coverage dist-tests/tests/**/*.test.js",
    "test:single": "npm run test:compile && node --test",
    "pretest": "npm run build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@types/node": "^24.8.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3"
  },
  "dependencies": {}
}
