import { test } from 'node:test';
import assert from 'node:assert';

test('Environment Setup: should run a test successfully', () => {
  assert.strictEqual(true, true, 'Expected true to be true');
});